const state = {
  awardName: {},
  deleteNum: 0
}

const mutations = {
  setAwardName: (state, arr) => state.awardName = arr,
  setDeleteNum: (state, arr) => state.deleteNum = arr
}

const actions = {

}

const getters = {

}

export default {
  state,
  getters,
  actions,
  mutations
}